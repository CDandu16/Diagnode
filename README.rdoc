== README

Things to do
1. file upload with ajax
2. getting buttons on navbar working
3. add groups
5. Making own comment engine
6. add cancan(almost done)
7. adding ability to do multiple forms properly
8. edit form that shows up with previous content


I think to run this you run  <tt>bundle install</tt> then <tt>rake db:migrate</tt> then <tt>rails server</tt>
